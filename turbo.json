{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "dev": {
      "cache": false
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "@moonwalker/artemis-cli:build": {
      "dependsOn": ["artemis-admin:build"],
      "outputs": []
    }
  }
}
